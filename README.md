# rylandgross.com

The static source for [rylandgross.com](http://rylandgross.com/), published with
GitHub Pages from the `main` branch.

To publish local changes, run `./publish-local.sh "Describe the update"`. The
script is deliberately Git-ignored, so it remains a local publishing helper.
